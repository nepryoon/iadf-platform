# db/

DDL canonica `iadf_sql_v1` (ADD §19): generata da TASK-02-FSM-CANONICAL e applicata due volte (idempotenza) su PostgreSQL 18 del profilo LOCAL-SYNTH, montata read-only in `/ddl` dentro il container `postgres`.
